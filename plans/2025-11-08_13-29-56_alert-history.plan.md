# Plan: Persistent Alert History

Add an in-app alert history so we can see every diagnostic message even if the UI freezes midway through a load.

## Tasks

- [x] Wire a lightweight log store (service or subject) inside the config admin area that records timestamped messages
  > Added [src/app/components/config-admin/config-alert.service.ts](../src/app/components/config-admin/config-alert.service.ts) to hold the alert history (up to 200 entries) with helper methods for info/warn/error plus console mirroring.
- [x] Surface the log history in the admin shell (e.g., collapsible panel) so users can review alerts chronologically
  > Updated [config-admin-shell.component.ts/html/css](../src/app/components/config-admin/) to subscribe to the alert stream, expose `alertMessages$`, add a trackBy helper, and render a Material card beneath the toolbar that lists timestamped entries with severity styling. Specs now describe the alert history panel.
- [x] Pipe key service events (document load start/end, blueprint fallbacks, errors) to the log helper and verify UI responsiveness
  > Injected `ConfigAlertService` into [config-workspace.service.ts](../src/app/components/config-admin/config-workspace.service.ts) so catalog refreshes, document loads, and blueprint fallbacks emit alerts (including summarized missing-reference warnings). Ran `npm run build` to ensure the instrumentation compiles (existing Sass/CommonJS warnings remain).
